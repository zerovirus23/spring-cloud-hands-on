---
server:
  port : 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zerovirus23/spring-cloud-hands-on
          #username: someUser
          #password: somePwd
          search-paths:
          - 'cloud-config-git'
          - 'cloud-config-git/src/main/resources/config'
          - 'cloud-config-git/src/main/resources/config/station*'
          